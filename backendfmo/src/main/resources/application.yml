server:
  address: 127.0.0.1
  port: 8081
  servlet:
    context-path: /api
spring:
  application:
    name: Backend FMO
  jpa:
    database-platform: com.backendfmo.db.dialect.SQLiteDialect
    show-sql: false
  datasource:
    url: jdbc:sqlite:src/main/java/com/backendfmo/db/test.db
    driver-class-name: org.sqlite.JDBC
