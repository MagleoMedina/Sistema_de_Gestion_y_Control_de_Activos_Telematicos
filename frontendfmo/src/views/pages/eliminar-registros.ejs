<!DOCTYPE html>
<html lang="es">

<head>
    <%- include('../partials/head') %>
    <title>Eliminar Registros</title>
    <style>
        /* --- PALETA FMO --- */
        :root {
            --fmo-red-dark: #dd0303;
            --fmo-red: #d6291c;
            --fmo-orange-red: #f25912;
            --fmo-orange: #fa812f;
            --fmo-yellow: #fab12f;
        }

        .tag-badge { 
            background-color: #000080;
            color: white; 
            padding: 1px 3px; 
            border-radius: 4px; 
            margin-right: 4px; 
            font-size: 10px;
        }
        
        .template-section { display: none; }
        .text-fmo-red { color: var(--fmo-red) !important; }

        /* Botón Principal */
        .btn-fmo {
            background: linear-gradient(to right, var(--fmo-red), var(--fmo-orange-red));
            border: none;
            color: white;
            transition: all 0.3s ease;
        }
        .btn-fmo:hover {
            background: linear-gradient(to right, var(--fmo-red-dark), var(--fmo-red));
            color: white;
            box-shadow: 0 4px 10px rgba(214, 41, 28, 0.3);
        }

        /* Tabla Estilizada */
        .table-custom thead {
            background: linear-gradient(135deg, var(--fmo-red-dark) 0%, var(--fmo-orange-red) 100%);
            color: white;
        }
        .table-custom tbody tr:hover {
            background-color: #fff5f5;
        }

        /* Modal de Peligro */
        .bg-gradient-fmo-danger {
            background: linear-gradient(135deg, var(--fmo-red-dark) 0%, #b30000 100%);
            color: white;
            border-bottom: 3px solid #8a0a0a;
        }
        
        .btn-danger-fmo {
            background-color: var(--fmo-red);
            border-color: var(--fmo-red);
            font-weight: bold;
        }
        .btn-danger-fmo:hover {
            background-color: var(--fmo-red-dark);
            border-color: var(--fmo-red-dark);
        }
    </style>
</head>

<body>
    <div class="wrapper">
        <%- include('../partials/sidebar') %>

        <div id="content" class="w-100 bg-light p-4 fade-in">
            
            <h4 class="mb-4 fw-bold text-fmo-red">
                <i class="bi bi-trash3-fill me-2" style="vertical-align: bottom;"></i>
                Gestión y Eliminación de Registros
            </h4>

            <div class="card shadow-sm border-0 mb-4">
                <div class="card-body p-4">
                    <div class="row g-3 align-items-end">
                        
                        <div class="col-md-3">
                            <label class="form-label fw-bold text-secondary">Tipo de Registro:</label>
                            <select class="form-select" id="selTipo">
                                <option value="equipos">Recibo de Equipos</option>
                                <option value="perifericos">Recibo de Periféricos</option>
                                <option value="daet">Entrega al DAET</option>
                                <option value="casos">Casos Resueltos</option> 
                            </select>
                        </div>

                        <div class="col-md-6">
                            <label class="form-label fw-bold text-secondary">Buscar por:</label>
                            <input type="text" class="form-control" id="inputBusqueda" placeholder="Ej: 119000, Solicitud ST...">
                        </div>

                        <div class="col-md-3">
                            <button class="btn btn-fmo w-100" onclick="buscarRegistros()">
                                <i class="bi bi-search me-1" style="vertical-align: bottom;"></i>Buscar
                            </button>
                        </div>
                    </div>
                </div>
            </div>

            <div class="card shadow-sm border-0">
                <div class="card-body p-0">
                    <div class="table-responsive">
                        <table class="table table-custom table-hover align-middle mb-0">
                            <thead class="text-center">
                                <tr>
                                    <th>#</th>
                                    <th id="th_identificador">Identificador</th>
                                    <th>Fecha</th>
                                    <th id="th_usuario">Usuario / Responsable</th>
                                    <th id="th_estatus">Estatus</th>
                                    <th>Acciones</th>
                                </tr>
                            </thead>
                            <tbody id="tablaResultados" class="text-center">
                                <tr>
                                    <td colspan="6" class="text-muted py-4">Utilice el buscador para encontrar registros.</td>
                                </tr>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>

        </div>
    </div>

    <div class="modal fade" id="modalVerDetalle" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog modal-xl"> 
            <div class="modal-content">
                <div class="modal-header bg-info text-white">
                    <h5 class="modal-title fw-bold"> <i class="bi bi-list-check me-2"></i>Vista Previa del Registro</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body bg-light">
                    
                    <div id="view_equipos" class="template-section">
                        <%- include('../partials/forms/template-equipos') %>
                    </div>

                    <div id="view_perifericos" class="template-section">
                        <%- include('../partials/forms/template-perifericos') %>
                    </div>

                    <div id="view_daet" class="template-section">
                        <%- include('../partials/forms/template-daet') %>
                    </div>

                    <div id="view_casos" class="template-section">
                        <div class="card border-0 shadow-sm">
                            <div class="card-header bg-white border-bottom-0 pb-0">
                                <h6 class="text-danger fw-bold text-uppercase"><i class="bi bi-person-lines-fill me-2"></i>Información del Caso</h6>
                            </div>
                            <div class="card-body">
                                <div class="row g-3">
                                    <div class="col-12 col-md-5">
                                        <label class="fw-bold text-muted small text-uppercase">Nombre Trabajador</label>
                                        <div class="fs-5 text-dark fw-semibold text-break" id="v_casos_nombre"></div>
                                    </div>
                                    <div class="col-6 col-md-3">
                                        <label class="fw-bold text-muted small text-uppercase">Ficha</label>
                                        <div class="fs-5 text-primary fw-bold" id="v_casos_ficha"></div>
                                    </div>
                                    <div class="col-6 col-md-4">
                                        <label class="fw-bold text-muted small text-uppercase">Fecha Atención</label>
                                        <div class="fs-5 text-dark" id="v_casos_fecha"></div>
                                    </div>

                                    <div class="col-12"><hr class="my-1 text-muted opacity-25"></div>

                                    <div class="col-12 col-md-6">
                                        <label class="fw-bold text-muted small text-uppercase">Gerencia / Dpto</label>
                                        <div class="text-secondary text-break" id="v_casos_gerencia"></div>
                                    </div>
                                    <div class="col-12 col-md-6">
                                        <label class="fw-bold text-muted small text-uppercase">Atendido Por</label>
                                        <div><span class="badge bg-secondary fs-6 text-wrap" id="v_casos_tecnico"></span></div>
                                    </div>

                                    <div class="col-12 mt-4">
                                        <label class="fw-bold text-muted small text-uppercase mb-2">Descripción del Reporte / Solución</label>
                                        <div class="p-3 bg-white border rounded shadow-sm" style="min-height: 100px;">
                                            <p class="mb-0 text-break" id="v_casos_reporte" style="white-space: pre-wrap; font-family: monospace, sans-serif; font-size: 0.95rem;"></p>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cerrar</button>
                </div>
            </div>
        </div>
    </div>

    <div class="modal fade" id="modalConfirmarDelete" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header bg-gradient-fmo-danger">
                    <h5 class="modal-title fw-bold">
                        <i class="bi bi-exclamation-triangle-fill me-2"></i>
                        Confirmar Eliminación
                    </h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body text-center p-4">
                    <div class="mb-3" style="font-size: 3rem; color: var(--fmo-red-dark);">
                        <i class="bi bi-trash3-fill"></i>
                    </div>
                    <p class="fs-5">Está a punto de eliminar el registro ID: <strong id="lblIdEliminar"></strong></p>
                    <p class="text-danger fw-bold">Esta acción es irreversible. ¿Está seguro?</p>
                </div>
                <div class="modal-footer justify-content-center">
                    <button type="button" class="btn btn-secondary px-4" data-bs-dismiss="modal">Cancelar</button>
                    <button type="button" class="btn btn-danger-fmo px-4" style="color:white" onclick="ejecutarEliminacion()">Sí, Eliminar</button>
                </div>
            </div>
        </div>
    </div>

    <%- include('../partials/modal') %>

    <script src="/js/scripts/eliminarRegistros.js"></script>
    <script src="/js/utils/api.js"></script>
    <script src="/js/bootstrap/bootstrap.bundle.min.js"></script>
    <script src="/js/utils/mappings.js"></script>
</body>
</html>