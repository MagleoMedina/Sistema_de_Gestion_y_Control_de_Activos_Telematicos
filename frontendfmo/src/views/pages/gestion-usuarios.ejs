<!DOCTYPE html>
<html lang="es">
<head>
    <%- include('../partials/head') %>
    <title>Gestión de Usuarios</title>
    <style>
        /* --- PALETA FMO --- */
        :root {
            --fmo-red-dark: #dd0303;
            --fmo-red: #d6291c;
            --fmo-orange-red: #f25912;
            --fmo-orange: #fa812f;
            --fmo-yellow: #fab12f;
        }

        /* Títulos */
        .text-fmo-title { color: var(--fmo-red-dark); }

        /* Botón Principal (Nuevo Usuario) */
        .btn-fmo {
            background: linear-gradient(to right, var(--fmo-red), var(--fmo-orange-red));
            border: none;
            color: white;
            transition: all 0.3s ease;
        }
        .btn-fmo:hover {
            background: linear-gradient(to right, var(--fmo-red-dark), var(--fmo-red));
            color: white;
            transform: translateY(-2px);
            box-shadow: 0 4px 10px rgba(214, 41, 28, 0.3);
        }

        /* Tabla Estilizada */
        .table-custom thead {
            background: linear-gradient(135deg, var(--fmo-red-dark) 0%, var(--fmo-orange-red) 100%);
            color: white;
        }
        .table-custom tbody tr:hover {
            background-color: #fff5f5; /* Fondo rosado muy pálido al hover */
        }

        /* Botón Eliminar (Rojo Intenso) */
        .btn-fmo-danger {
            background-color: white;
            color: var(--fmo-red);
            border: 1px solid var(--fmo-red);
            transition: all 0.2s;
        }
        .btn-fmo-danger:hover {
            background-color: var(--fmo-red);
            color: white;
        }

        /* Modal Headers */
        .modal-header-fmo {
            background: linear-gradient(to right, var(--fmo-red), var(--fmo-orange));
            color: white;
        }
        .modal-header-danger {
            background: linear-gradient(135deg, var(--fmo-red-dark), #b30000);
            color: white;
        }
        
        /* Badges */
        .badge-admin {
            background-color: var(--fmo-red-dark);
            color: white;
        }
        .badge-soporte {
            background-color: var(--fmo-orange);
            color: #333;
        }
    </style>
</head>

<body>
    <div class="wrapper">
        <%- include('../partials/sidebar') %>
        <div id="content" class="w-100 bg-light p-4 fade-in">
        
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h4 class="m-0 fw-bold text-fmo-title">
                    <i class="bi bi-people-fill me-2"></i>
                    Gestión de Usuarios del Sistema
                </h4>
                <button class="btn btn-fmo shadow-sm px-4 rounded-pill" data-bs-toggle="modal" data-bs-target="#modalCrearUsuario">
                    <i class="bi bi-person-plus-fill" style="vertical-align: bottom; margin-right: 5px;"></i>Nuevo Usuario
                </button>
            </div>

            <div class="card shadow-sm border-0">
                <div class="card-body p-0">
                    <div class="table-responsive">
                        <table class="table table-custom table-hover align-middle mb-0">
                            <thead class="text-center">
                                <tr>
                                    <th>ID</th>
                                    <th>Nombre de Usuario (Username)</th>
                                    <th>Tipo / Rol</th>
                                    <th>Acciones</th>
                                </tr>
                            </thead>
                            <tbody id="tablaUsuarios" class="text-center">
                                <tr>
                                    <td colspan="4" class="text-muted py-4">Cargando usuarios...</td>
                                </tr>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>

        </div>
    </div>

    <div class="modal fade" id="modalCrearUsuario" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header modal-header-fmo">
                    <h5 class="modal-title fw-bold">
                        <i class="bi bi-person-add me-2"></i>Registrar Nuevo Usuario
                    </h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <form id="formUsuario">
                        <div class="mb-3">
                            <label class="form-label fw-bold text-fmo-title">Username:</label>
                            <input type="text" class="form-control" id="inputUsername" placeholder="Ej: jpererez" required>
                        </div>
                        <div class="mb-3">
                            <label class="form-label fw-bold text-fmo-title">Contraseña:</label>
                            <input type="text" class="form-control" id="inputClave" placeholder="******" required>
                        </div>
                        <div class="mb-3">
                            <label class="form-label fw-bold text-fmo-title">Tipo de Usuario:</label>
                            <select class="form-select" id="selectTipo">
                                <option value="SOPORTE">SOPORTE</option>
                                <option value="ADMIN">ADMINISTRADOR</option>
                            </select>
                        </div>
                    </form>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                    <button type="button" class="btn btn-fmo" onclick="guardarUsuario()">Guardar Usuario</button>
                </div>
            </div>
        </div>
    </div>

    <div class="modal fade" id="modalConfirmarEliminacion" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header modal-header-danger">
                    <h5 class="modal-title fw-bold">Confirmar Eliminación</h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body text-center p-4">
                    <div class="mb-3 text-danger">
                        <i class="bi bi-exclamation-triangle-fill" style="font-size: 3rem; color: #dd0303;"></i>
                    </div>
                    <p class="fs-5">¿Está seguro de que desea eliminar al usuario <b id="lblUsuarioEliminar" class="text-danger"></b>?</p>
                    <small class="text-muted">Esta acción no se puede deshacer.</small>
                </div>
                <div class="modal-footer justify-content-center">
                    <button type="button" class="btn btn-secondary px-4" data-bs-dismiss="modal">Cancelar</button>
                    <button type="button" class="btn btn-danger px-4" style="background-color: #dd0303; border-color: #dd0303;" onclick="ejecutarEliminacion()">Sí, Eliminar</button>
                </div>
            </div>
        </div>
    </div>

    <%- include('../partials/modal') %>

    <script src="/js/utils/api.js"></script>
    <script src="/js/bootstrap/bootstrap.bundle.min.js"></script>
    <script src="/js/scripts/gestionUsuarios.js"></script>
</body>
</html>