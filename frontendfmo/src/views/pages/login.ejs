<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Acceso al Sistema | CSV Ferrominera</title>

    <link href="/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/bootstrap-icons/bootstrap-icons.css">
    
    <link rel="icon" type="image/x-icon" href="/favicon.ico">
    <link rel="icon" type="image/png" href="/img/ferro.png">
    
    <style>
        /* --- PALETA DE COLORES FMO ---
           Rojo Intenso: #dd0303
           Rojo Std:     #d6291c
           Naranja Rojo: #f25912
           Naranja:      #fa812f
           Amarillo/Naranja: #fab12f
        */

        /* --- ESTILOS VISUALES Y ANIMACIONES --- */
        body {
            /* Degradado completo usando tu paleta */
            background: linear-gradient(135deg, #dd0303 0%, #d6291c 40%, #fa812f 100%);
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            overflow: hidden;
        }

        /* Burbujas de fondo (ligeramente doradas para combinar) */
        .bg-circles {
            position: absolute;
            top: 0; left: 0; width: 100%; height: 100%;
            overflow: hidden;
            z-index: 0;
        }
        .bg-circles li {
            position: absolute;
            display: block;
            list-style: none;
            width: 20px; height: 20px;
            background: rgba(250, 177, 47, 0.3); /* #fab12f con transparencia */
            animation: animate 25s linear infinite;
            bottom: -150px;
        }
        .bg-circles li:nth-child(1){ left: 25%; width: 80px; height: 80px; animation-delay: 0s; }
        .bg-circles li:nth-child(2){ left: 10%; width: 20px; height: 20px; animation-delay: 2s; animation-duration: 12s; }
        .bg-circles li:nth-child(3){ left: 70%; width: 20px; height: 20px; animation-delay: 4s; }
        .bg-circles li:nth-child(4){ left: 40%; width: 60px; height: 60px; animation-delay: 0s; animation-duration: 18s; }
        .bg-circles li:nth-child(5){ left: 65%; width: 20px; height: 20px; animation-delay: 0s; }
        
        @keyframes animate {
            0%{ transform: translateY(0) rotate(0deg); opacity: 1; border-radius: 0; }
            100%{ transform: translateY(-1000px) rotate(720deg); opacity: 0; border-radius: 50%; }
        }

        /* --- TARJETA DE LOGIN --- */
        .login-card {
            background: rgba(255, 255, 255, 0.96); /* Blanco casi puro */
            backdrop-filter: blur(10px);
            border-radius: 20px;
            border-top: 5px solid #dd0303; /* Detalle superior rojo intenso */
            padding: 40px;
            width: 100%;
            max-width: 420px;
            box-shadow: 0 15px 35px rgba(168, 14, 14, 0.3); /* Sombra rojiza suave */
            position: relative;
            z-index: 10;
            animation: slideIn 0.8s cubic-bezier(0.2, 0.8, 0.2, 1);
        }

        @keyframes slideIn {
            from { transform: translateY(50px); opacity: 0; }
            to { transform: translateY(0); opacity: 1; }
        }

        /* --- STATUS INDICATOR (Se mantiene funcional verde/rojo) --- */
        .server-status-container {
            position: absolute;
            top: -20px;
            left: 50%;
            transform: translateX(-50%);
            background: white;
            padding: 8px 20px;
            border-radius: 30px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
            display: flex;
            align-items: center;
            gap: 10px;
            font-weight: bold;
            font-size: 0.9rem;
            white-space: nowrap;
            z-index: 20;
        }

        .status-dot { width: 12px; height: 12px; border-radius: 50%; display: inline-block; }
        .status-on .status-dot { background-color: #28a745; box-shadow: 0 0 0 rgba(40, 167, 69, 0.4); animation: pulse-green 2s infinite; }
        .status-on .status-text { color: #28a745; }
        .status-off .status-dot { background-color: #d6291c; /* Usamos el rojo de la paleta */ box-shadow: 0 0 0 rgba(214, 41, 28, 0.4); animation: pulse-red 2s infinite; }
        .status-off .status-text { color: #d6291c; }

        @keyframes pulse-green {
            0% { box-shadow: 0 0 0 0 rgba(40, 167, 69, 0.7); }
            70% { box-shadow: 0 0 0 10px rgba(40, 167, 69, 0); }
            100% { box-shadow: 0 0 0 0 rgba(40, 167, 69, 0); }
        }
        @keyframes pulse-red {
            0% { box-shadow: 0 0 0 0 rgba(221, 3, 3, 0.7); }
            70% { box-shadow: 0 0 0 10px rgba(221, 3, 3, 0); }
            100% { box-shadow: 0 0 0 0 rgba(221, 3, 3, 0); }
        }

        /* --- INPUTS PERSONALIZADOS CON LA PALETA --- */
        .form-floating > .form-control:focus ~ label,
        .form-floating > .form-control:not(:placeholder-shown) ~ label {
            color: #d6291c; /* Rojo al enfocar */
            font-weight: bold;
        }
        
        .form-control:focus {
            border-color: #fab12f; /* Borde Naranja/Dorado */
            box-shadow: 0 0 0 0.25rem rgba(250, 177, 47, 0.25); /* Glow Naranja */
        }

        /* --- BOTÓN CON DEGRADADO PERSONALIZADO --- */
        .btn-login {
            background: linear-gradient(to right, #d6291c, #fa812f); /* Rojo a Naranja */
            border: none;
            padding: 12px;
            font-size: 1.1rem;
            transition: all 0.3s ease;
        }
        .btn-login:hover {
            transform: scale(1.02);
            background: linear-gradient(to right, #dd0303, #f25912); /* Un poco más oscuro al hover */
            box-shadow: 0 5px 15px rgba(214, 41, 28, 0.4);
        }
        
        /* Ajuste para input group del password */
        .input-group-text {
            border-left: none;
            background-color: white;
            border-top-right-radius: 0.375rem !important;
            border-bottom-right-radius: 0.375rem !important;
        }
        .form-control.border-end-0 { border-right: none; }
    </style>
</head>
<body>

    <ul class="bg-circles">
        <li></li><li></li><li></li><li></li><li></li><li></li><li></li><li></li><li></li><li></li>
    </ul>

    <div class="login-card">
        
        <div id="statusContainer" class="server-status-container status-checking" style="border: 1px solid #fab12f;">
            <span class="status-dot"></span>
            <span id="statusText" class="status-text">CONECTANDO...</span>
        </div>

        <div class="text-center mb-4 mt-3">
            <img src="/img/ferro.png" alt="Logo" width="90" class="mb-3" style="filter: drop-shadow(0 4px 6px rgba(0,0,0,0.1));">
            
            <h3 class="fw-bold" style="color: #dd0303;">Bienvenido</h3>
            <p class="text-muted small">Sistema de Gestión Telemática</p>
        </div>

        <form id="loginForm">
            
            <div class="form-floating mb-3">
                <input type="text" class="form-control" id="username" name="username" placeholder="Usuario" required>
                <label for="username">Nombre de Usuario</label>
            </div>

            <div class="input-group mb-4">
                <div class="form-floating flex-grow-1">
                    <input type="password" class="form-control border-end-0" id="password" name="password" placeholder="Contraseña" required>
                    <label for="password">Contraseña</label>
                </div>
                <span class="input-group-text" style="cursor: pointer;" id="btnTogglePassword">
                    <i class="bi bi-eye-fill fs-5" style="color: #f25912;" id="iconEye"></i>
                </span>
            </div>

            <div class="d-grid gap-2">
                <button type="submit" class="btn btn-primary btn-login text-white fw-bold rounded-pill" id="btnLogin">
                   Iniciar Sesión
                </button>
            </div>

        </form>

        <div class="text-center mt-4">
            <small class="text-muted">© 2026 Ing. Magleo Medina</small>
        </div>
    </div>
    
    <script src="/js/utils/api.js"></script>
    <script src="/js/scripts/login.js"></script>
    <script src="/js/bootstrap/bootstrap.bundle.min.js"></script>
</body>
</html>