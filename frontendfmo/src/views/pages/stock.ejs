<!DOCTYPE html>
<html lang="es">

<head>
    <%- include('../partials/head') %>
    <title>Inventario y Stock</title>
    <style>
        /* --- PALETA FMO --- */
        :root {
            --fmo-red-dark: #dd0303;
            --fmo-red: #d6291c;
            --fmo-orange-red: #f25912;
            --fmo-orange: #fa812f;
            --fmo-yellow: #fab12f;
        }

        /* Tarjetas de Resumen */
        .stock-card {
            border: none;
            border-radius: 15px;
            transition: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
            overflow: hidden;
            position: relative;
            border-left: 5px solid var(--fmo-red);
        }
        .stock-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(221, 3, 3, 0.15) !important;
        }
        .icon-bg {
            position: absolute;
            right: -20px;
            bottom: -20px;
            font-size: 5rem;
            opacity: 0.15;
            transform: rotate(-15deg);
            color: var(--fmo-red);
        }

        /* Botones Circulares de Acción */
        .btn-circle {
            width: 30px;
            height: 30px;
            padding: 0;
            border-radius: 50%;
            text-align: center;
            font-size: 14px;
            line-height: 1.42857;
            transition: transform 0.2s;
        }
        .btn-circle:hover { transform: scale(1.2); }

        /* Badge de Stock Bajo (Amarillo FMO) */
        .badge-low-stock {
            background-color: var(--fmo-yellow);
            color: #333;
            font-weight: bold;
            animation: pulse-yellow 2s infinite;
        }
        @keyframes pulse-yellow {
            0% { box-shadow: 0 0 0 0 rgba(250, 177, 47, 0.7); }
            70% { box-shadow: 0 0 0 10px rgba(250, 177, 47, 0); }
            100% { box-shadow: 0 0 0 0 rgba(250, 177, 47, 0); }
        }

        /* Tabla Estilizada (Gradiente Rojo) */
        .table-custom thead {
            background: linear-gradient(135deg, var(--fmo-red-dark), var(--fmo-orange-red));
            color: white;
        }
        .table-custom tbody tr:hover {
            background-color: #fff5f5; /* Fondo rosado muy pálido al hover */
        }

        /* Textos y Botones Personalizados */
        .text-fmo-primary { color: var(--fmo-red); }
        .text-fmo-dark { color: #8a0a0a; }

        .btn-fmo {
            background: linear-gradient(to right, var(--fmo-red), var(--fmo-orange-red));
            border: none;
            color: white;
        }
        .btn-fmo:hover {
            background: linear-gradient(to right, var(--fmo-red-dark), var(--fmo-red));
            color: white;
            box-shadow: 0 4px 10px rgba(214, 41, 28, 0.3);
        }

        /* Tabs Personalizados */
        .nav-tabs .nav-link.active {
            color: var(--fmo-red-dark);
            border-top: 3px solid var(--fmo-red);
            font-weight: bold;
        }
        .nav-tabs .nav-link {
            color: #666;
        }
    </style>
</head>

<body>
    <div class="wrapper">
        <%- include('../partials/sidebar') %>

        <div id="content" class="w-100 bg-light p-4">
            
            <div class="d-flex justify-content-between align-items-center mb-4 fade-in">
                <div>
                    <h4 class="m-0 fw-bold text-dark">
                        <i class="bi bi-clipboard-data-fill" style="vertical-align: bottom;"></i>
                        Control de Stock y Repuestos
                    </h4>
                    <p class="text-muted small m-0">Gestiona las entradas y salidas de material.</p>
                </div>
                <button class="btn btn-fmo shadow-sm" onclick="abrirModalAgregar()">
                    <i class="fas fa-plus"></i> <i class="bi bi-node-plus-fill" style="vertical-align: bottom; margin-right: 5px;"></i>Agregar Nuevo Item
                </button>
            </div>

            <div class="row g-4 mb-4 fade-in">
                <div class="col-md-4">
                    <div class="card stock-card bg-white shadow-sm p-3 h-100">
                        <div class="d-flex justify-content-between align-items-center position-relative z-1">
                            <div>
                                <h6 class="text-muted text-uppercase fw-bold" style="font-size: 0.8rem;">Total Items</h6>
                                <h2 class="fw-bold text-primary mb-0" id="totalItems">0</h2>
                            </div>
                            <div class="icon-bg text-primary"><i class="bi bi-table" style = "color: #000;"></i></div>
                        </div>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="card stock-card bg-danger text-white shadow-sm p-3 h-100">
                        <div class="d-flex justify-content-between align-items-center position-relative z-1">
                            <div>
                                <h6 class="text-white-50 text-uppercase fw-bold" style="font-size: 0.8rem;">Stock Crítico</h6>
                                <h2 class="fw-bold text-white mb-0" id="totalCritico">0</h2>
                            </div>
                            <div class="icon-bg text-white">
                                <i class="bi bi-exclamation-triangle-fill" style="color: yellow;"></i>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="card shadow-sm border-0 fade-in">
                <div class="card-header bg-white border-bottom-0 pt-3 px-4">
                    <ul class="nav nav-tabs card-header-tabs" id="stockTabs" role="tablist">
                        <li class="nav-item" role="presentation">
                            <button class="nav-link active fw-bold" id="comp-tab" data-bs-toggle="tab" data-bs-target="#tab-componentes" type="button" role="tab">
                                <i class="bi bi-pc-display-horizontal" style = "vertical-align: bottom;"></i>
                                Componentes Internos
                            </button>
                        </li>
                        <li class="nav-item" role="presentation">
                            <button class="nav-link fw-bold" id="peri-tab" data-bs-toggle="tab" data-bs-target="#tab-perifericos" type="button" role="tab">
                                <i class="bi bi-keyboard" style="vertical-align: bottom; margin-right: 3px;"></i>Periféricos
                            </button>
                        </li>
                    </ul>
                </div>
                
                <div class="card-body p-0">
                    <div class="tab-content" id="stockTabsContent">
                        
                        <div class="tab-pane fade show active" id="tab-componentes" role="tabpanel">
                            <div class="table-responsive">
                                <table class="table table-custom align-middle mb-0">
                                    <thead>
                                        <tr>
                                            <th>Item</th>
                                            <th>Marca / Caracteristicas</th>
                                            <th class="text-center">Stock Actual</th>
                                            <th class="text-center">Estado</th>
                                            <th class="text-center">Acciones</th>
                                        </tr>
                                    </thead>
                                    <tbody id="tablaComponentes">
                                    </tbody>
                                </table>
                            </div>
                        </div>

                        <div class="tab-pane fade" id="tab-perifericos" role="tabpanel">
                            <div class="table-responsive">
                                <table class="table table-custom align-middle mb-0">
                                    <thead>
                                        <tr>
                                            <th>Item</th>
                                            <th>Marca / Caracteristicas</th>
                                            <th class="text-center">Stock Actual</th>
                                            <th class="text-center">Estado</th>
                                            <th class="text-center">Acciones</th>
                                        </tr>
                                    </thead>
                                    <tbody id="tablaPerifericos">
                                    </tbody>
                                </table>
                            </div>
                        </div>

                    </div>
                </div>
            </div>

        </div>
    </div>

    <div class="modal fade" id="modalStock" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header bg-primary text-white">
                    <h5 class="modal-title fw-bold"><i class="bi bi-file-earmark-zip-fill" style="vertical-align: bottom; margin-right: 3px;"></i>Ingreso de Inventario</h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <form id="formStock">
                        
                        <div class="mb-3">
                            <label class="form-label fw-bold">Categoría:</label>
                            <select class="form-select" id="selCategoria" onchange="cargarListaItems()">
                                <option value="COMPONENTE">Componentes Internos (RAM, Disco, etc.)</option>
                                <option value="PERIFERICO">Periféricos (Mouse, Teclado, etc.)</option>
                            </select>
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-bold">Tipo de Artículo:</label>
                            <select class="form-select" id="selReferencia" required>
                                <option value="">Seleccione...</option>
                            </select>
                            <div class="form-text">Listado basado en la categoría seleccionada.</div>
                        </div>

                        <div class="row g-2 mb-3">
                            <div class="col-6">
                                <label class="form-label fw-bold">Marca:</label>
                                <input type="text" class="form-control" id="inputMarca" placeholder="Ej: Kingston" required>
                            </div>
                            <div class="col-6">
                                <label class="form-label fw-bold">Modelo/Caracteristicas:</label>
                                <input type="text" class="form-control" id="inputModelo" placeholder="Ej: 8GB DDR4" required>
                            </div>
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-bold">Cantidad Inicial:</label>
                            <input type="number" class="form-control" id="inputCantidad" value="1" min="1" required>
                        </div>
                    </form>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                    <button type="button" class="btn btn-primary" onclick="guardarNuevoStock()">Guardar</button>
                </div>
            </div>
        </div>
    </div>

    <div class="modal fade" id="modalConfirmarEliminacion" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header bg-danger text-white bg-gradient-fmo-danger">
                    <h5 class="modal-title fw-bold">Confirmar Eliminación</h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body text-center p-4">
                    <div class="mb-3 text-danger">
                        <i class="bi bi-exclamation-triangle-fill" style="font-size: 3rem;"></i>
                    </div>
                    <p class="fs-5">¿Está seguro de que desea eliminar este registro del inventario?</p>
                    <small class="text-muted">Esta acción es irreversible y afectará los reportes.</small>
                </div>
                <div class="modal-footer justify-content-center">
                    <button type="button" class="btn btn-secondary px-4" data-bs-dismiss="modal">Cancelar</button>
                    <button type="button" class="btn btn-danger px-4" onclick="ejecutarEliminacionStock()">Sí, Eliminar</button>
                </div>
            </div>
        </div>
    </div>

    <%- include('../partials/modal') %>

    <script src="/js/utils/api.js"></script>
    <script src="/js/bootstrap/bootstrap.bundle.min.js"></script>
    <script src="/js/scripts/stock.js"></script>
</body>
</html>